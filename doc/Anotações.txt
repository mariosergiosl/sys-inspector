
#INICIAR
# 1. Vá para a pasta do projeto (Código Fonte)
cd /opt/host/Syncfolder/Trabalho/GitHub/mariosergiosl/sys-inspector

# 2. Ative o Venv (que está no disco nativo da VM)
source /root/venvs/sys-inspector/bin/activate

# 3. Verifique se ativou (o prompt deve mudar para (sys-inspector)...)
# E execute o teste gerando o relatório
sudo python3 src/inspector.py --html logs/relatorio_net.html --duration 10




# TESTAR

# Lista os processos suspeitos
ps -ef | grep -E "bad_actor|kworker_fake"

# Mata sem piedade
sudo pkill -9 -f "bad_actor"
sudo pkill -9 -f ".kworker_fake"
